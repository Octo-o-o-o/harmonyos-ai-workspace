# 第三方评审 v2 存档 · 基于源码（2026-05-06）

> 第二轮评审：评审者把 8 个项目全 clone 到 `~/WorkSpace/Reference/harmonyos-survey/`，**基于实际源码**而非 README 描述做判断。比 v1 评审更精确，纠正了 v1 的 5 个错判。
>
> 本文档归档**评审结论 + 处置决策表**。配套 v1 在 `REVIEW-2026-05-06.md`。

---

## v2 vs v1 关键修正

| # | v1 错判 | v2 源码事实 |
| --- | --- | --- |
| 1 | "B 没有钩子" | **B 有完整钩子体系**：`skills/harmonyos-ark/hooks/` 含 arkts-scan.sh/.ps1、acl-check.sh/.ps1、claude-settings.json、copilot-hooks.json + 152 行三平台格式对照表 |
| 2 | "A 有 60+ 编号反模式" | **自动扫描层只有 13 条**；review checklist 36 条 + AGC 拒因 20 条 + OHPM 黑名单 11 条加起来才 ≈ 67 条（**分布在 4 个层**） |
| 3 | "A 的 5 skill 是 rules/review/publishing/ecosystem/migration" | 实际是 `arkts-rules / build-debug / harmonyos-review / signing-publish / state-management`（已对齐，PLAN.md 实际并未误命名——v2 评审者此条记错） |
| 4 | "C 真有 4208 行 SKILL.md" | **属实** |
| 5 | "G 只是 hdc 启动 app/装包/列设备" | G 实际暴露 **16 个 MCP 工具**：含 click / long_click / swipe / input_text / get_uilayout / get_screenshot——已经是 UI 自动化栈级别 |

---

## 处置决策表

### P0（v2 评审给的"必做"）

| 评审项 | 处置 | 提交 |
| --- | --- | --- |
| **P0-1** PLAN.md 5 skill 命名同步 | **核查后无需改** —— PLAN.md 实际未误命名（评审记错或读到旧版）；存档此次核查 | 无改动 |
| **P0-2** README 第 27 行"PostToolUse 钩子是核心差异化"改写 | **采纳** —— 改写为"PostToolUse 钩子链路"，并新增"真正独有的能力 vs 同类项目"段，把真实差异点（OHPM 四级校验 / AGC 稳定 ID / awk 去注释 / 文档镜像 / fixture 回归）显式列出 | README "内置内容"段重写 |
| **P0-3** README 顶部加"前置依赖" | **早已实施** —— 上一轮（v1 评审采纳）已加"这是给谁用的？"段含 4 工具安装表 | 无新改动 |
| **P0-4** CLAUDE.md 顶部嵌"AI 必读硬约束" | **早已实施** —— CLAUDE.md 第 0 节就是硬约束 + § 0.5 状态铁律 + § 2.5 Skills 触发索引 | 无新改动 |

### P1

| 评审项 | 处置 | 提交 |
| --- | --- | --- |
| **P1-1** scan-arkts 扩到 36 条覆盖（KIT/PERM/STATE 等） | **部分采纳** —— 只补**5 条最有把握**的（KIT-001、PERM-001、STATE-009、SEC-001、COMPAT-001、ARKTS-016 空 catch），跳过 grep 假阳性高的；从 13 → 18 | `tools/hooks/lib/scan-arkts.sh` 末尾加 5 条 |
| **P1-2** 加 PowerShell 版 scan-arkts.ps1 + check-ohpm-deps.ps1 | **不采纳** —— Windows 鸿蒙开发者大都走 WSL；脚本全重写工作量过大；v0.3 真用户提需求再做 | — |
| **P1-3** hooks 加 Copilot/Codex 配置示例 | **采纳** —— 但定位调整：Codex/Cursor 没有等价的 PostToolUse hook，**真正能给的是 git pre-commit + AGENTS.md 软引导**。新建 `tools/hooks/examples/` 含三个示例（pre-commit / Copilot 占位 / GitHub Action）+ README 说清楚工具能力差异 | `tools/hooks/examples/` 4 个新文件 |
| **P1-4** 接入或 fork G | **不采纳直接接入 / 采纳指引文档** —— G 半年未更新 + 锁 Python 3.13 + system_prompt.py 空文件，vendor 后维护成本未知；改为提供 `docs/MCP-INTEGRATION.md` 完整对接指引，让感兴趣的用户自己 fork。同时给出"不用 G 也能等价"的 hdc + Bash 替代方案 | `docs/MCP-INTEGRATION.md` |
| **P1-5** 拆 5 skill 重切为 3 层 | **不采纳** —— v1 评审已论证：F 的分层是绑百度 SDK 域；按问题域切更稳定；P0-3 frontmatter 已用"激活/不激活"显式条件解决 token 节省 | — |
| **P1-6** OHPM 黑/白名单拆外部文件 + 扩展 | **采纳** —— 拆出 `tools/data/ohpm-blacklist.txt`（**26 条**：React/Vue/axios/lodash/moment/firebase/sentry 等 AI 高频虚构包）+ `ohpm-whitelist.txt`（22 条核心包） | `tools/data/ohpm-{blacklist,whitelist}.txt` |

### P2

| 评审项 | 处置 | 提交 |
| --- | --- | --- |
| **P2-1** 加 minimal empty-project 模板 | **不采纳** —— 5 分钟自测已用 mkdir+heredoc 跑通；不与 DevEco "New Project" 重复 | — |
| **P2-2** bootstrap 加 y/N + README 区分基础/完整 | **采纳** —— bootstrap 脚本加交互式 `read -r -p` 提示（默认 N，需显式 y），并解释"本仓库核心能力不依赖文档镜像"；同时支持 `-y/--yes` 跳过；README 区分早已实施 | `tools/bootstrap-upstream-docs.sh` |
| **P2-3** scan-arkts 加 `--json` 模式 | **采纳** —— 加 `--json` flag，输出结构化 JSON 数组（含 rule/severity/file/line/snippet/reason 字段），CI 友好；保持文本模式向后兼容 | `tools/hooks/lib/scan-arkts.sh` |
| **P2-4** 给 36 个 ID 加 fixture | **不采纳** —— 多日工作量，让 P1-1 扩展规则后再补；v0.3 候选 | — |

### 诚信修订（非评审给但事实需要）

| 项 | 处置 |
| --- | --- |
| README "60+ 编号"自夸 | **修正** —— 新增"规则编号体系（精确说明）"段：13 自动化 + 36 review + 20 AGC + 26 OHPM = 各层独立的 95 条编号规则；明确表示"不应被合并表述为 X 条"。**这是评审者最重要的诚信反馈**。 |

---

## v2 修订前后对比

| 维度 | v1 提交后状态 | v2 修订后状态 |
| --- | --- | --- |
| 自动扫描规则 | 13 条 | **18 条**（+ KIT-001/PERM-001/STATE-009/SEC-001/COMPAT-001/ARKTS-016） |
| OHPM 黑/白名单 | 内联 11 + 21 项 | 外部文件 + 内联兜底，**26 + 22 项** |
| 多工具 hook 集成 | 仅 Claude Code | **Claude Code 强校验 + Codex/Cursor pre-commit + Copilot 占位 + GitHub Action** |
| `--json` 输出 | 无 | **有**（CI / 第三方接入） |
| 文档镜像默认行为 | 直接拉 2.7 GB | **交互式 y/N**（明确提示非必要） |
| MCP 接入路径 | 仅只读 | **指引文档**：含动作型接入步骤 + 替代方案 + 维护监测 |
| README "核心差异化"叙事 | "PostToolUse 钩子" | **5 条真独占能力**显式列出 + 规则编号体系精确化 |

---

## v2 评审者全文摘录（结构性观点保留）

```
A 真正的市场定位应该是：
"鸿蒙 AI 工作区里最轻、AI-first 设计最深、提审本土化最实在"
——而不是 README 现在隐含的"什么都做"。
把 P0 改完后，README 自我描述会比现在准确得多。

A 在 8 项目中已经是"工程化最重"的那一个，但 README 表达层和 AI-first
文档顺序仍偏"给人读的项目主页"而非"给 AI 注入的规则"——这是它当前
最容易被对比项反超的弱点。

工程化深度：A 与 B 并列第一（A 深、B 广）
知识体量：F > C > B > E > A > H > D > G——A 实际只有 8,765 行
AI-first 设计：A、D 共同最强（D 强制 cite source；A 钩子+OHPM+AGC 三件套）
闭环完整性：A 唯一接入 MCP 但仅只读；接入 G 之后将是唯一端到端项目
规模适合度：A 740K 是 8 家中最轻的工作区形态（B 2.6M / F 3.6M）
```

完整原文见上下文中评审者输出。

---

## 后续追注

- 2026-05-06 / 07：v2 评审采纳的 P0/P1/P2 全部实施完成
- 下次评审建议：等真用户用 1-2 周（按 PLAN.md M4 节奏）后做
